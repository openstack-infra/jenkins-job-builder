builders:
  - trigger-builds:
    - project: "build_started"
      parameter-factories:
        - factory: filebuild
          file-pattern: propfile*.txt
          no-files-found-action: NOPARMS
      block: true
